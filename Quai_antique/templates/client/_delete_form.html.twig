<form method="post" action="{{ path('app_client_delete', {'id': user.id}) }}" onsubmit="return confirm('êtez-vous sûre de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">Supprimer le compte</button>
</form>
